# normify-vrcnotifier · Normify Outline

> 派生索引（每次 normify_build 重建）。AI 导航入口：先广度后深度。

## vrcnotifier（https://github.com/ssldxss/vrcnotifier.git）

- vrcnotifier — VRChat 好友监控通知器 / VRChat Friend Monitor & Notifier — VRChat 好友监控与通知服务：WebSocket 实时事件 + REST 定时对账，QQ 机器人推送 2FA/邀请/公告/好友动态，Web 面板管理监控与设… — [模块 164 · API 362]
  - vrcnotifier.app — 应用装配与启动 / Application Wiring & Boot — 组合根：读取环境变量与运行参数、解析访问令牌与主密钥、按依赖顺序装配数据库/日志/头像/VRChat/QQ/监控/HTTP 各组服务，并提供进程启动自检与优雅退… — [模块 14 · API 16]
    - vrcnotifier.app.config — 配置与凭据解析 / Configuration & Credential Resolution — 把进程环境与命令行开关归一成应用可用的配置：环境变量读取、访问令牌解析（环境变量 → 数据库 → 旧 token.txt 迁移 → 随机生成）与运行参数默认值。 — [模块 4 · API 4]
      - vrcnotifier.app.config.env — 环境变量读取 / Environment Variable Reading — 统一的环境变量读取小工具：空串视为未设置（回落到默认值），并提供整数解析（解析失败同样回落）。所有可配置项（端口、阈值、外部地址）都从这里取值。 — [模块 1 · API 2]
      - vrcnotifier.app.config.parameters — 运行参数装配 / Runtime Parameter Assembly — 把环境变量与调用方覆盖项合并成运行时参数对象：VRChat API/WS 地址、User-Agent、CORS 来源、2FA 会话 TTL、加密模式，以及监控（… — [模块 1 · API 1]
      - vrcnotifier.app.config.token — 访问令牌解析与迁移 / Access Token Resolution & Migration — 按优先级解析 Web 面板访问令牌：环境变量 ACCESS_TOKEN → 数据库 settings.access_token → 旧版 data/token.… — [模块 1 · API 1]
    - vrcnotifier.app.frontend-server — 前端静态服务器 / Frontend Static Server — 独立前端进程（node serve.js）：用原生 http 托管 public/ 目录，内含 MIME 表、目录穿越防护（normalize 后必须落在根目录… — [模块 1 · API 3]
    - vrcnotifier.app.lifecycle — 进程生命周期 / Process Lifecycle — main() 的启动、运行与退出三段：密钥解析与解密自检（密钥不符就清库重启）、启动 HTTP 监听与 QQ/自动登录恢复，以及 SIGINT/SIGTERM … — [模块 4 · API 4]
      - vrcnotifier.app.lifecycle.main — 主流程编排 / Main Entry Orchestration — 组装并启动服务：生成日志与令牌、建立运行标识行、按参数构建应用、监听端口、启动 QQ 机器人（按已保存账号）、触发自动登录恢复，并把首次 WS 连接成功后的令牌… — [模块 1 · API 1]
      - vrcnotifier.app.lifecycle.shutdown — 优雅退出 / Graceful Shutdown — 响应 SIGINT/SIGTERM（双击则强制退出）：依次推送停止通知、停监控与看门狗定时器、停头像清理定时器、停健康探测、停全部 QQ 机器人、停用活跃账号，… — [模块 1 · API 1]
      - vrcnotifier.app.lifecycle.startup — 启动自检与密钥校验 / Boot Self-check & Key Validation — 打开数据库前解析主密钥（Docker Secret → MASTER_KEY → --no-encrypt），随后用当前密钥探测敏感密文是否可解：不可解则清空除… — [模块 1 · API 2]
    - vrcnotifier.app.wiring — 依赖装配 / Dependency Wiring — buildApplication 的装配三个阶段：基础服务（数据库/日志流/文件日志/头像缓存/事件总线/会话表）→ 领域服务（VRChat 客户端、世界名、Q… — [模块 4 · API 5]
      - vrcnotifier.app.wiring.foundation — 基础服务装配 / Foundation Service Wiring — 创建存储与横切基础设施并注入给后续模块：SQLite 仓储（带可选加密）、内存日志流并接管全局、多段文件日志（可选）、头像缓存目录与定时清理、事件总线 Even… — [模块 1 · API 3]
      - vrcnotifier.app.wiring.http — HTTP 装配与令牌打码 / HTTP Wiring & Token Masking — 把全部协作者与配置交给 createApp 得到 Express 应用、自动登录入口与连接状态查询，并把它们回填到 QQ 指令所需的只读钩子上；同时建立日志流令… — [模块 1 · API 1]
      - vrcnotifier.app.wiring.services — 领域服务装配 / Domain Service Wiring — 按调用方向装配全部领域服务并打通回调：VRChat REST 客户端工厂 → 世界名查询 → QQ 指令与机器人 → 通知器 → WS 管线（回指监控处理消息）… — [模块 1 · API 1]
  - vrcnotifier.data — 数据持久化与状态机 / Persistence & State Machine — 持久化与领域状态机：同步 SQLite 仓储（含表结构、迁移与加密列），以及负责状态派生、转移分类与位置标签解析的纯函数模块。 — [模块 16 · API 50]
    - vrcnotifier.data.cache — 世界名与群组名缓存 / World & Group Name Caches — 两个刻意不对称的小缓存。世界名缓存只存成功解析到的名字，失败冷却记在内存，因此其失败相关列在迁移中被删除，启动时也会清理占位行以免被当成真名字。群组名缓存仍保留… — [模块 1 · API 4]
    - vrcnotifier.data.crypto-fields — 敏感字段加密与清库 / Encrypted Fields & Wipe — 仓储层唯一的加密语义所有者。读取时用把值绑定到行身份的 AAD 解密用户敏感列，从而防止密文在行之间被置换。探测接口报告是否存在用当前密钥解不开的密文，启动流程… — [模块 1 · API 3]
    - vrcnotifier.data.dedupe — 通知去重窗口 / Notification Dedupe Window — 按复合字符串键的时间窗口去重表。写入键会刷新其时间戳，并在表超过上限后按时间裁剪最旧的行，因此无需后台任务即可保持有界。窗口由调用方传入，使好友变更与站内通知能… — [模块 1 · API 2]
    - vrcnotifier.data.friend-config — 逐好友通知配置 / Per-Friend Notification Config — 逐好友通知开关原先存在独立表，如今挂在好友行上，与好友同生共死。其默认值体现了产品规则：新加好友默认不通知——列默认全 0，且迁移只把真正存在过配置行的好友的值… — [模块 1 · API 1]
    - vrcnotifier.data.friend-state — 好友状态机 / Friend State Machine — 纯粹的好友状态机：由位置或快照派生状态、把转移分类为变更类型与通知字段，以及防止连接抖动产生虚假下线通知的待确认阶梯。 — [模块 4 · API 5]
      - vrcnotifier.data.friend-state.apply — 待确认阶梯与变更负载 / Pending Confirmation & Change Payload — 待确认阶梯是这个模块真正的价值所在。入参字段为 undefined 表示保留旧值，显式 null 才有含义。怀疑下线不会立刻通知：先记录 pending 状态与… — [模块 1 · API 2]
      - vrcnotifier.data.friend-state.derive — 在线状态派生 / Presence State Derivation — 带明确优先级的在线状态派生。有快照时以三个状态数组为准，因为它们比好友行上的 location 字段更准；只有三个数组都没提到该好友时才由位置字符串决定。基于位… — [模块 1 · API 2]
      - vrcnotifier.data.friend-state.transition — 状态转移分类 / Transition Classification — 把一对在线状态翻译成意图。它覆盖了真正会被在意的情形：进游戏算上线，只上网页算较轻的上线，而退出游戏但网页仍在线同样算下线。世界编号变化总按切换世界上报，pri… — [模块 1 · API 1]
    - vrcnotifier.data.friends — 好友行与状态写入 / Friend Rows & State Writes — friends 表是全局的而非按账号划分，因为同一时间只跑一个 VRChat 账号。upsert 对资料字段做 COALESCE，使稀疏负载不会把已存头像清空，… — [模块 1 · API 6]
    - vrcnotifier.data.location — 位置标签解析 / Location Tag Parsing — 解析 VRChat 的 location 标签形状 world:instance~flag(value)。它把世界 id 与实例后缀分开，识别表示并非真实实例的… — [模块 1 · API 2]
    - vrcnotifier.data.purge — 账号数据清除与重置 / Account Data Purge & Reset — 按归属划分的销毁路径。登出总会清除属于该 VRChat 账号的一切，因为它们对下一个账号毫无意义；清缓存与清设置是在此之上的两个独立选项。清设置同样保留访问令牌… — [模块 1 · API 4]
    - vrcnotifier.data.qq-binding — QQ 机器人绑定 / QQ Bot Bindings — 把 QQ 应用映射到已与其绑定的 openid，主动消息据此知道该发往何处。绑定是全局的而非按 VRChat 账号划分，因此迁移重建了该表以删除旧的用户列，并按… — [模块 1 · API 2]
    - vrcnotifier.data.schema — 表结构与迁移 / Schema & Migrations — 仓储的声明式核心：七条 CREATE TABLE 语句，加上驱动迁移与设置白名单的常量，以及打开时执行的迁移过程——把任何旧库收敛到当前结构（补列、删已退役列、… — [模块 1 · API 10]
    - vrcnotifier.data.settings — 全局设置与渠道配置 / Global Settings & Channel Config — 带类型白名单的全局键值设置：未知键在读写两侧都被忽略，整数截断后以字符串存储，QQ AppSecret 用固定 AAD 加密。两个启动迁移把历史通知列从 use… — [模块 1 · API 4]
    - vrcnotifier.data.users — 账号与凭据持久化 / Users & Credential Persistence — users 表及其凭据策略。凭据刻意保持全局唯一：保存 cookie jar 会清掉其他行的 cookie、保存的用户名与记住我标志，因为同一时间只监控一个 V… — [模块 1 · API 7]
  - vrcnotifier.infra — 基础设施 / Cross-cutting Infrastructure — 不含领域知识的横切基础设施：三层日志栈（日志器、内存环、分段文件）、带密钥解析的 AES-256-GCM 字段加密、磁盘头像缓存，以及若干共享小工具。 — [模块 14 · API 33]
    - vrcnotifier.infra.avatar — 头像缓存 / Avatar Cache — 头像缓存，其核心想法是“文件系统即索引”：文件存在即命中，其修改时间即最后访问时间，无需建表。它只回答本地有没有、要不要去上游取；发字节是 HTTP 层的事。 — [模块 4 · API 13]
      - vrcnotifier.infra.avatar.maintenance — 缓存清理与淘汰 / Cache Sweeping & Eviction — 让缓存不会无限增长，用修改时间同时充当 TTL 时钟与访问新鲜度。文件数上限只在下载之后检查，且先做一次目录列举即短路，因此昂贵的逐项 stat 排序只在真正超… — [模块 1 · API 4]
      - vrcnotifier.infra.avatar.naming — 缩略图 URL 与 key 规则 / Thumbnail URL & Key Rules — 让缓存 key、下载地址与磁盘文件名三者保持一致的命名规则。VRChat 返回的头像有原图与缩略图两种形态、尺寸也随它；本模块把两种形态都改写成统一尺寸的规范缩… — [模块 1 · API 4]
      - vrcnotifier.infra.avatar.store — 下载与原子落盘 / Download & Atomic Store — 单张图片的下载与落盘。同一 key 的并发请求会合并为一次下载；下载先写临时文件再重命名就位，使读取方永远不会看到写了一半的图片。写入前要求响应为 200、声明… — [模块 1 · API 5]
    - vrcnotifier.infra.crypto — 数据加密 / Data Encryption — AES-256-GCM，每个值用随机 IV，并把附加认证数据绑定到字段与行身份，因此从某行取出的密文无法被重放到另一行。版本前缀让旧明文直通，并让密钥不符或密文… — [模块 1 · API 4]
    - vrcnotifier.infra.logging — 日志栈 / Logging Stack — “日志”背后的三层协作：负责格式化与分发的日志器、供面板实时流使用的有界内存环，以及用稳定序号保存完整历史的分段文件。 — [模块 5 · API 13]
      - vrcnotifier.infra.logging.file-segments — 分段文件日志 / Segmented File Log — 分段文件日志，其序号是文件名与段内行偏移的纯函数。行的身份因此跨重启稳定、对段淘汰免疫，且文件里不需要嵌入任何编号，文本保持干净。启动必定新开一段，段满按字节或… — [模块 1 · API 6]
      - vrcnotifier.infra.logging.logger — 日志器与行格式 / Logger & Line Format — 把每行格式化为时间、级别、分类与正文。分类取自行首的方括号标签，使旧调用点继续可用，并用别名表把中英文标签归一到同一套词表。它先写可选的文件日志以便在那里推导序… — [模块 1 · API 2]
      - vrcnotifier.infra.logging.memory-stream — 内存日志流 / In-memory Log Stream — 保存最近若干行的环形缓冲与订阅者集合，是面板实时日志视图的数据来源。序号可由外部提供（文件层用文件名与行偏移推导），使内存与磁盘在重启后对同一行达成一致；流内记… — [模块 1 · API 4]
      - vrcnotifier.infra.logging.time — 本地时间格式化 / Local Time Formatting — 一个极小但影响面很广的函数：从日志行到停止通知，产品中所有面向人的时间戳都按服务器本地时间渲染而非 UTC，因为阅读它们的人处在一个时区，并习惯与自己的表对照。 — [模块 1 · API 1]
    - vrcnotifier.infra.util — 共享工具 / Shared Helpers — 两个被许多模块依赖的独立小工具：令牌打码与信任等级推导，以及提前用兜底值兑现而不拒绝的 promise 超时。 — [模块 3 · API 3]
      - vrcnotifier.infra.util.deadline — Promise 超时兜底 / Promise Deadline — 把项目的立场编码成工具：等待是调用方自己的事——原始 promise 不被取消，定时器 unref，超时以兜底结果兑现而不是拒绝。这正是慢速世界名查询能先交还缓… — [模块 1 · API 1]
      - vrcnotifier.infra.util.identity — 令牌打码与信任等级 / Token Masking & Trust Levels — 打码刻意做到既安全又有用：较长的令牌保留前四后四位，使运维能确认当前用的是哪一个；短到这种展示就会泄露的令牌则整体打码。信任等级推导按固定优先级把平台标签集收敛… — [模块 1 · API 2]
  - vrcnotifier.monitor — 好友监控编排 / Friend Monitoring Orchestration — 监控编排层：WS 事件分发、REST 快照对账、状态机落地、通知去重与 WS 看门狗。持有会话表与下线确认定时器，把三类输入（实时事件、快照、pending 校… — [模块 25 · API 37]
    - vrcnotifier.monitor.core — 监控装配与共享状态 / Monitor Assembly & Runtime State — 工厂装配与共享运行态：依赖注入、日志器与事件总线选择、会话表及 running/awaitingSnapshot/pendingStatus/pendingBu… — [模块 1 · API 2]
    - vrcnotifier.monitor.fault — 故障窗口与启停通知 / Fault Window & Lifecycle Notices — 断线与 401 的统一故障语义（首次连接不算故障）：故障开始时打时间戳并挂定时器，只有持续超过阈值才推送一次故障通知，之后若恢复则补发一条恢复说明。订阅 ws-… — [模块 1 · API 4]
    - vrcnotifier.monitor.group-name — 群组名缓存与退避 / Group Name Cache & Backoff — 为公告通知解析群名。先查缓存（成功名有效期 1 小时；缓存为未知群组时按 retry_at 决定是否复用），未命中优先批量拉取用户群组灌缓存（区分成员关系 id… — [模块 1 · API 2]
    - vrcnotifier.monitor.notify — 通知分发 / Notification Dispatch — 决定什么会真正打扰用户：好友状态变更的逐条去重与分发、VRChat 站内通知的解析与分发，以及把状态变化与紧随其后的切世界合并成一条的窗口。 — [模块 4 · API 6]
      - vrcnotifier.monitor.notify.change-dispatch — 好友变更分发与去重 / Friend Change Dispatch & Dedupe — 好友变更通知的唯一收口。没有总开关：每条变更自带要检查的 notifyField，新好友默认全关。去重 key 包含新旧状态与世界，避免同一好友的不同变化被吞掉… — [模块 1 · API 1]
      - vrcnotifier.monitor.notify.coalesce — 变更类型映射与合并 / Change Type Mapping & Coalescing — 把“发生了什么变化”与“怎么报出去”分开。来自 friend-update 的状态变化先压合并窗口；若窗口内同好友的 friend-location 到达且新状… — [模块 1 · API 2]
      - vrcnotifier.monitor.notify.vrc-notification — 站内通知解析与推送 / VRChat Notification Parsing & Dispatch — 处理 VRChat 站内通知并复用同一通知渠道，但只放行三类：世界邀请、戳一戳与群组公告。类型开关读全局设置，且必须在去重标记之前判定，否则“关掉再打开”会补推… — [模块 1 · API 3]
    - vrcnotifier.monitor.pipeline-router — WS 事件路由 / WS Event Router — WS 侧唯一入口及其门禁：重连再同步、每帧必须通过的四道准入检查，以及按事件类型映射到好友/自身/通知三条处理链。 — [模块 5 · API 4]
      - vrcnotifier.monitor.pipeline-router.dispatch — 帧准入与分派 / Frame Admission & Dispatch — 每一帧在被处理前要过四道检查：会话存在、用户不在等待对账、用户仍在库中、帧带 content。通知生命周期事件（see/hide/response/clear/… — [模块 1 · API 1]
      - vrcnotifier.monitor.pipeline-router.friend-events — 好友事件映射 / Friend Event Mapping — 把七种好友事件翻译为在线状态输入：online 与 location 解析位置标签（真实实例、private 哨兵、traveling 沿用旧世界）并解析世界名… — [模块 1 · API 1]
      - vrcnotifier.monitor.pipeline-router.reconnect — 重连再同步 / Reconnect Resynchronisation — 重连意味着本地画面可能已经过时，因此把该用户放入等待对账集合并立即触发全量对账；在对账完成之前丢弃所有 WebSocket 消息并记一条日志。用少量消息丢失换取… — [模块 1 · API 1]
      - vrcnotifier.monitor.pipeline-router.self-events — 自身事件映射 / Self Event Mapping — 处理两个指向自己的事件类型。user-update 只带资料字段与社交状态；user-location 带完整用户对象与位置：状态由位置派生，offline 或… — [模块 1 · API 1]
    - vrcnotifier.monitor.session — 会话注册与用户启停 / Session Registry & Activation — 持有 vrchat_user_id 到会话的映射。激活时注册会话、按显示名连接 WS 管线，并立即执行一次只建基线、不发通知的首次对账；停用时断开连接并清理该用… — [模块 1 · API 4]
    - vrcnotifier.monitor.snapshot — 快照对账 / Snapshot Reconciliation — REST 全量对账，权威画面：它也是恢复判定中“API 返回 200”这一半的唯一来源，因此定时触发与每次重连都要走它。 — [模块 4 · API 3]
      - vrcnotifier.monitor.snapshot.auth-401 — 对账 401 分流 / Reconciliation 401 Branching — VRChat 的 401 并不只有一种：Missing Credentials 表示 cookie 作废（典型是换 IP），需要带密码完整重登；Unauthor… — [模块 1 · API 1]
      - vrcnotifier.monitor.snapshot.progress — 对账进度上报 / Reconciliation Progress Reporting — 按语义阶段上报登录进度，而不绑定具体 HTTP 请求——密码直登与 2FA 两条路的请求序列不同，绑请求会让等待页错位。好友拉取百分比始终以名册总数为分母累加在… — [模块 1 · API 1]
      - vrcnotifier.monitor.snapshot.run — 全量对账主流程 / Snapshot Reconciliation Run — 一次 me() 同时获得三个在线状态数组、好友名册与自身在线状态。随后在线与活动好友用一次分页请求取详情，离线好友零请求（首次对账额外拉一次离线名册以补全资料）… — [模块 1 · API 1]
    - vrcnotifier.monitor.state — 状态落地 / State Persistence — 从输入到落库之间的全部逻辑：好友写入唯一入口、自身在线状态写入入口，以及把怀疑下线批量复核确认的流程。 — [模块 4 · API 6]
      - vrcnotifier.monitor.state.friend-apply — 好友输入落地 / Friend Input Persistence — 好友数据入库的唯一入口。首见好友直接按现状插入，不比较也不通知；否则先更新资料字段，再由状态机差分在线字段。把结果 dbUpdate 连同实例号（undefin… — [模块 1 · API 1]
      - vrcnotifier.monitor.state.pending-verification — 下线待定批量确认 / Offline Pending Verification — 针对疑似下线的防闪烁确认。同一账号的全部 pending 好友共享一个定时器与一次 me() 调用；到期点取“最后到达加确认延迟”与“最早 pending 加两… — [模块 1 · API 3]
      - vrcnotifier.monitor.state.self-presence — 自身在线状态落地 / Self Presence Persistence — 自己的在线状态只入库并推给面板，绝不进入通知链、去重或 pending 逻辑。状态派生刻意把 offline/空位置归为网页在线，因为此时网页会话仍在活动；pr… — [模块 1 · API 2]
    - vrcnotifier.monitor.timers — 定时器生命周期 / Timer Lifecycle — 自动对账是滑动窗口：任何一次对账触发都会清掉旧定时器并把下一次推到完整间隔之后，因此频繁活动只会顺延而不会叠加。启动是幂等的，同时装上 watchdog 间隔；… — [模块 1 · API 3]
    - vrcnotifier.monitor.watchdog — WS 静默看门狗 / WS Silence Watchdog — VRChat 管线除协议层 ping 外没有应用层心跳，因此用“沉默”判断存活：已连接但最后一条消息早于 watchdog 窗口的用户会被强制重连，而重连路径会… — [模块 1 · API 1]
    - vrcnotifier.monitor.world-name — 世界名取用 / World Name Access — 监控层对世界名服务的薄适配。缓存、查询与失败兜底都在 world 模块，这里只决定“等多久”与哨兵值处理：同步 peek 从不发请求；异步查询套一层时限，超时先… — [模块 1 · API 2]
  - vrcnotifier.qq — QQ 机器人推送 / QQ Bot Delivery — QQ 官方机器人投递：网关连接、凭据处理、限速发送、以在线列表作答的聊天指令，以及通知分发与 Markdown 消息模板。 — [模块 13 · API 32]
    - vrcnotifier.qq.bot — QQ 机器人连接 / QQ Bot Connection — QQ 官方机器人（bot.q.qq.com OpenAPI，非 OneBot）：令牌获取、按绑定串行的 C2C 发送、带心跳与 identify 的网关连接、首… — [模块 5 · API 17]
      - vrcnotifier.qq.bot.registry — 机器人注册与生命周期 / Bot Registry & Lifecycle — 持有机器人注册表并负责与已存设置调和。启动时为每个已存账号启动机器人；sync 对比目标配置与运行现状，相应连接、重连或停止，并返回变更计数使设置页能如实反馈。… — [模块 1 · API 5]
      - vrcnotifier.qq.bot.sender — 消息发送与重试 / Message Sending & Retry — 唯一的发送收口。所有主动消息都经过按绑定 key 串行的 promise 链，使两条通知永远不会竞争，并保持最小发送间隔。网络错误、429 与 5xx 会重试，… — [模块 1 · API 4]
      - vrcnotifier.qq.bot.socket — 网关连接与心跳 / Gateway Socket & Heartbeat — 网关状态机。Hello 携带心跳间隔并触发 identify；op 0 分发 READY 与两个 C2C 消息事件；op 11 确认心跳，若连续三个间隔无确认则… — [模块 1 · API 5]
      - vrcnotifier.qq.bot.token — 访问令牌与凭据处理 / Access Token & Credential Handling — 获取并缓存机器人访问令牌，在过期前留出安全余量刷新，并把并发刷新合并到同一个 promise 上。被服务端明确判定为无效的凭据按终止处理：清空 QQ 设置并停止… — [模块 1 · API 3]
    - vrcnotifier.qq.commands — 聊天指令 / Chat Commands — 聊天侧行为：把好友表渲染成可读的在线列表，以及同时充当 2FA 验证码入口的指令处理。 — [模块 3 · API 5]
      - vrcnotifier.qq.commands.handler — 指令处理与世界名补全 / Command Handler & World Fill — 指令处理把 2FA 验证码入口放在最前：等待验证期间先消费验证码回复并直接返回，根本不会去构造在线列表——这正是重登过程中直接发六位数字就能生效的原因。在线好友… — [模块 1 · API 2]
      - vrcnotifier.qq.commands.online-list — 在线列表渲染 / Online List Rendering — 构造对任意聊天消息的回复：特别关注优先，其后是其他在线好友，世界尽量解析成名字，同时输出 Markdown 表格与按显示宽度对齐的纯文本（中日韩字符按两列计）。… — [模块 1 · API 3]
    - vrcnotifier.qq.notifier — 通知渠道分发 / Notification Fan-out — 通知渠道层，目前只有 QQ，但按扇出结构编写，增加第二个渠道只需多一个 key 而不必重写。它从全局设置读取开关与凭据，经模板模块渲染变更，以 Markdown… — [模块 1 · API 3]
    - vrcnotifier.qq.templates — 消息模板 / Message Templates — 把变更对象变成人读消息：占位符引擎、中文标签表、Markdown 转义，以及把“变化的”排在“没变的”之前的行序规则。 — [模块 3 · API 7]
      - vrcnotifier.qq.templates.message — 消息组装 / Message Assembly — 以两种形态组装最终消息：站内通知走自己的模板并只保留非空行；好友状态变更则先输出加粗的社交状态行，再输出加粗的变化字段，然后是不加粗的未变化字段与时间。模板模式… — [模块 1 · API 3]
      - vrcnotifier.qq.templates.vars — 模板变量与标签 / Template Variables & Labels — 所有消息共用的渲染词汇。它把机器值映射为中文标签（三种在线状态、四种社交状态），对昵称与世界名等用户可控内容转义 Markdown 元字符，并把变更展开为自定义… — [模块 1 · API 4]
  - vrcnotifier.server — HTTP 服务与 REST API / HTTP Server & REST API — 全项目唯一的 Express 5 HTTP 面：VRChat 登录与 2FA、会话恢复、好友列表与逐好友通知配置、全局设置、测试通知、监控状态、日志查询、唯一 … — [模块 29 · API 62]
    - vrcnotifier.server.access — 引导配置与访问校验 / Bootstrap Config & Access Verification — 面板在拿到令牌之前需要的两个免鉴权端点：GET /api/config 返回是否需要令牌、各延迟阈值、加密状态与版本；POST /api/access/veri… — [模块 1 · API 2]
    - vrcnotifier.server.auth — 登录认证与会话 / Authentication & Session — VRChat 认证：密码登录与 2FA 挑战移交、2FA 完成、会话落地、登出、自动登录恢复，以及自动重登状态机。 — [模块 8 · API 23]
      - vrcnotifier.server.auth.auto-login — 自动登录恢复 / Automatic Login Recovery — 用持久化的 cookie 恢复已保存会话：成功即落地；失败按可重试（401/429/网络/5xx）与终止分类。401 且存有密码时升级到重登引擎而不退避；coo… — [模块 1 · API 6]
      - vrcnotifier.server.auth.login — 密码登录 / Password Login — POST /api/login：校验入参、清理过期的 pending 2FA 记录，用新的无 jar 客户端登录；VRChat 要求 2FA 时存入临时会话（密… — [模块 1 · API 2]
      - vrcnotifier.server.auth.relogin — 自动重登 / Automatic Re-login — cookie 失效（换 IP/会话挂起）时用已保存密码自动重登：每用户状态表、每小时滚动频控、指数退避，以及网页弹窗与 QQ 共用的验证码校验通道。 — [模块 3 · API 9]
        - vrcnotifier.server.auth.relogin.loop — 重登循环与退避 / Re-login Loop & Backoff — 按 VRCX 同款方式重登：先复用旧 cookie jar 带上上下文，再用保存的密码登录。遇到 2FA 挑战则挂起并同时经 QQ 与 SSE 2fa-need… — [模块 1 · API 4]
        - vrcnotifier.server.auth.relogin.two-factor — 重登验证码与 QQ 指令 / Re-login 2FA & QQ Command — 重登的验证侧：运行中会话被挂起时建立待验证会话（从响应体或 401 错误体中取可用验证方式），提供网页弹窗与 QQ 指令钩子共用的验证码校验，并可在保留密码的前… — [模块 1 · API 5]
      - vrcnotifier.server.auth.session — 会话落地与登出 / Session Finalization & Lifecycle — 密码登录、登录 2FA、自动重登与挂起重验证四种入口共用的唯一会话落地点：重置退避状态、停用旧会话、换账号时清掉他人数据、写入用户行、按记住我保存或清除 coo… — [模块 1 · API 5]
      - vrcnotifier.server.auth.two-factor — 登录两步验证 / Login Two-Factor Verification — POST /api/login/2fa：取出待验证会话，校验验证码（邮箱验证码按 xxxx-xxxx 格式化），重新读取当前用户，再用登录时暂存的凭据落地会话。… — [模块 1 · API 1]
    - vrcnotifier.server.avatar — 头像缓存服务 / Avatar Serving — 挂载在 /api/avatar 的头像服务链：未配置缓存时直接 404；否则依次是登录守卫（401）、按需 ensure(key)（编码非法 400、下载失败 … — [模块 1 · API 1]
    - vrcnotifier.server.friends — 好友路由 / Friend Routes — 好友读取与配置路由：GET /api/friends 返回全部好友的前端就绪序列化结果（世界名从缓存补，缺失则后台触发查询，不阻塞响应）；PUT /api/fr… — [模块 1 · API 2]
    - vrcnotifier.server.logs — 后端日志接口 / Backend Log API — 面板的后端日志访问：服务端等级/分类筛选，以及三种取数形态的分页查询端点。 — [模块 3 · API 3]
      - vrcnotifier.server.logs.filter — 日志筛选谓词 / Log Filter Predicates — 解析结构化日志行 [时间] [级别] [分类] 正文并施加多选筛选语义：省略某维度即不过滤，空值不匹配任何行（显式清空选择会全部隐藏），无法解析的行恒匹配以便前… — [模块 1 · API 2]
      - vrcnotifier.server.logs.query — 日志查询端点 / Log Query Endpoint — GET /api/logs 有三种形态：before=seq&limit 从分段日志文件向前翻历史，并继续扫描直到凑满一页匹配行；after=seq 补 SSE… — [模块 1 · API 1]
    - vrcnotifier.server.middleware — HTTP 中间件管线 / HTTP Middleware Pipeline — 按注册顺序的请求管线及收尾：JSON 解析、CORS、Bearer 令牌门、未知 API 404、静态托管与兜底错误处理。 — [模块 4 · API 5]
      - vrcnotifier.server.middleware.access-token — 访问令牌鉴权门 / Access Token Gate — Bearer 访问令牌门。未配置令牌时整体跳过；仅作用于 /api；白名单为 /api/config 与 /api/access/verify。路径先小写并去尾… — [模块 1 · API 2]
      - vrcnotifier.server.middleware.error-handler — 兜底错误处理 / Fallback Error Handler — Express 兜底错误处理：记录失败并回 500 JSON；若响应头已发出则跳过写回，避免破坏已部分写出的响应。 — [模块 1 · API 1]
      - vrcnotifier.server.middleware.json-cors — 请求体解析与 CORS / Body Parsing & CORS — 请求体解析与跨域策略：express.json 限制 1MB；CORS 头（来源可配，默认全放行）允许 GET/POST/PUT/DELETE/OPTIONS，… — [模块 1 · API 2]
    - vrcnotifier.server.monitor-control — 对账与测试通知路由 / Snapshot & Test Notification Routes — 两个运维动作：POST /api/monitor/snapshot 立即跑一次对账（不重试，失败回 502，返回快照时间），让面板的刷新按钮如实反映结果；POS… — [模块 1 · API 2]
    - vrcnotifier.server.runtime — 应用装配与共享运行态 / App Composition & Runtime State — HTTP 层的组合根：注入协作者，派生共享的事件总线/世界名/日志引用，声明单用户运行态（当前会话、pending 2FA 表、重登表、SSE 客户端集、退避参… — [模块 1 · API 2]
    - vrcnotifier.server.serialization — 响应序列化与脱敏 / Response Serialization & Scrubbing — 把数据库行转成前端零解析的响应，并在出站时脱敏：补世界名（同步 peek 缓存）、头像缓存 key 与嵌套好友配置，从用户行剔除 cookie_data/密码，… — [模块 1 · API 8]
    - vrcnotifier.server.settings — 设置路由 / Settings Routes — 全局设置路由：GET 返回白名单设置，QQ AppSecret 打码；PUT 同时接受 camelCase 与 snake_case，掩码值视为保持原值、空值视… — [模块 1 · API 2]
    - vrcnotifier.server.sse — 服务端事件推送（SSE） / Server-Sent Events — 面向浏览器的唯一长连接事件通道：SSE 客户端集、帧封装与死连接摘除、日志流转发、事件总线到 SSE 的桥接，以及每秒 WS 消息数推送。 — [模块 4 · API 6]
      - vrcnotifier.server.sse.broadcast — SSE 广播与日志转发 / SSE Broadcast & Log Forwarding — 维护 SSE 客户端集合，按 event/data 两行封装事件；广播时写失败即从集合摘除。订阅后端日志流，把每行实时转发为 log（令牌行被替换时用 log-… — [模块 1 · API 3]
      - vrcnotifier.server.sse.bus-bridge — 事件总线桥接与 WS 统计 / Bus to SSE Bridge & WS Stats — 订阅监控事件总线并把领域事件转发到浏览器：snapshot、notification、world-name、session-expired、ws-failure… — [模块 1 · API 2]
      - vrcnotifier.server.sse.events-endpoint — SSE 事件端点 / SSE Events Endpoint — 唯一的长连接端点。写入 text/event-stream 头（并关闭代理缓冲），先发 : connected 注释帧，把响应登记进客户端集，用 25 秒 : … — [模块 1 · API 1]
    - vrcnotifier.server.static — 前端静态托管 / Static UI Hosting — 可选的一体化部署模式：提供 publicDir 时后端同时托管面板静态资源。它注册在未知 API 404 之后，因此永远不会遮蔽 API 路由。 — [模块 1 · API 1]
    - vrcnotifier.server.status — 状态与遥测路由 / Status & Telemetry Routes — 状态与遥测路由：statusPayload 汇总登录态、打码后的当前用户、活跃账号、WS 连接与最后消息时间、QQ 状态、上次快照时间、待验证 2FA 数与延迟… — [模块 1 · API 5]
  - vrcnotifier.vrc — VRChat 接入 / VRChat Integration — 与 VRChat 对话的全部能力：带 cookie jar 与限流的 REST 客户端、WebSocket 管线、按需世界名服务，以及 API 健康探测与官方服… — [模块 18 · API 45]
    - vrcnotifier.vrc.api — VRChat REST 客户端 / VRChat REST Client — 带登录态的 VRChat REST 客户端：滑动窗口限流与有界指数退避的请求传输层、登录与身份接口，以及社交读取接口。 — [模块 4 · API 13]
      - vrcnotifier.vrc.api.auth — 登录与身份接口 / Login & Identity Calls — 客户端的身份部分：密码登录并自动识别 2FA 挑战（邮箱验证码会重新格式化为 VRChat 期望的 xxxx-xxxx），验证码校验，当前用户与任意用户资料读取… — [模块 1 · API 5]
      - vrcnotifier.vrc.api.social — 好友/世界/群组接口 / Friend, World & Group Calls — 社交读取接口。好友列表比较特殊：VRChat 把在线与离线好友分在两个查询里，因此该方法按需分页拉取名册，并用 onPage 回调上报累计条数，使等待页能显示如… — [模块 1 · API 4]
      - vrcnotifier.vrc.api.transport — 请求传输与限流退避 / Request Transport & Throttling — 所有 VRChat 调用底下的传输层。请求带上目标 URL 的 cookie 头，并按一分钟滑动窗口限流；429、网络错误与 5xx 按指数退避加抖动重试，次数… — [模块 1 · API 4]
    - vrcnotifier.vrc.cookiejar — Cookie 会话保持 / Cookie Session Jar — 为 fetch 客户端实现的极简浏览器式 cookie jar：解析 Set-Cookie 属性，同名同域同路径的 cookie 直接替换而不堆叠，按域后缀、路… — [模块 1 · API 4]
    - vrcnotifier.vrc.health — API 健康探测 / API Health Probe — 通过无凭据探测公开 config 端点持续测量 VRChat API 延迟（health 端点会 401 因此不可用）。每轮取若干计时样本，舍去超时者并对其余取… — [模块 1 · API 4]
    - vrcnotifier.vrc.pipeline — WebSocket 管线 / WebSocket Pipeline — VRChat 实时管线客户端：连接生命周期与重连、帧解析与去重、协议层 ping/pong 保活、故障升级，以及每秒消息统计。 — [模块 5 · API 14]
      - vrcnotifier.vrc.pipeline.connection — 连接与重连保活 / Connection & Reconnect Lifecycle — 每用户一条连接的生命周期。VRChat 协议没有应用层心跳，因此存活判断依赖协议 ping 与 pong 超时（超时即终止僵尸连接），以及有界指数退避的重连。持… — [模块 1 · API 4]
      - vrcnotifier.vrc.pipeline.control — 连接控制接口 / Connection Control API — 管理器的对外方法面。forceReconnect 刻意只负责关闭套接字，把通知与重连安排全部交给 close 处理函数，使强制重连与自然断线走同一条路径——否则… — [模块 1 · API 5]
      - vrcnotifier.vrc.pipeline.frames — 帧解析与串行队列 / Frame Parsing & Serial Queue — 两个看似不起眼但很关键的行为。其一，帧会与同一连接上的上一帧去重（基线在 open 时清空，避免新连接的首帧被吞），且 content 为字符串时会再解析一次，… — [模块 1 · API 3]
      - vrcnotifier.vrc.pipeline.stats — 消息速率统计 / Message Rate Statistics — 保留一分钟的每秒消息计数，供面板的 WebSocket 速率图表使用。序列通过遍历最近 60 个秒桶生成，使图表拿到定宽数组且空缺补零；读写时都会清理过期桶以保… — [模块 1 · API 2]
    - vrcnotifier.vrc.status — VRChat 服务状态 / VRChat Service Status — 仿 VRCX 上报 VRChat 自身的服务健康。它是惰性的——只有面板询问时才发请求——结果缓存一分钟，并发调用共享同一次在途请求。全部正常映射为 norma… — [模块 1 · API 1]
    - vrcnotifier.vrc.world — 世界名按需查询 / World Name Lookup — 全项目唯一的“世界编号换显示名”入口，分为无状态传输层与负责缓存、合并、冷却与兜底策略层两部分。 — [模块 5 · API 9]
      - vrcnotifier.vrc.world.cache — 缓存、冷却与请求合并 / Cache, Cooldown & Coalescing — 缓存之上的策略：成功名有效期 1 小时；同一世界的并发调用共享同一次在途请求；刚完成的结果在合并窗口内复用。连续失败转入冷却，从 5 分钟逐次翻倍到 1 小时封… — [模块 1 · API 4]
      - vrcnotifier.vrc.world.resolve — 查询与失败兜底 / Lookup & Failure Fallback — 真正的查询与失败分类。404/403 是永久性的，立刻进入冷却；429 是在说“别打了”，同样直接冷却而不重试；网络、超时与 5xx 才就地重试一次，仍失败再冷… — [模块 1 · API 2]
      - vrcnotifier.vrc.world.scheduler — 并发与速率调度 / Concurrency & Rate Scheduler — 挡在每次世界请求前的小调度器：同时在途不超过 N 个，每分钟不超过 M 次。仅因速率被挡住时，它会睡到最旧的时间戳滑出窗口再继续，而不是忙等，因此一批好友同时换… — [模块 1 · API 2]
      - vrcnotifier.vrc.world.transport — 无 Cookie 世界查询 / Cookie-free World Fetch — 通过公开 API 按编号查询世界，不带 cookie 也不带 Authorization，因此与登录会话彻底解耦，不会因 cookie 挂起而失效。失败统一归一… — [模块 1 · API 1]
  - vrcnotifier.web — Web 前端面板 / Web Panel — 浏览器面板：零依赖单页应用，负责发现并鉴权后端、驱动登录与 2FA、带动画地渲染好友列表、展示实时日志与 WebSocket 速率，并通过四行等待页完成启动。 — [模块 34 · API 87]
    - vrcnotifier.web.app — 面板应用 / Panel Application — 面板应用本体：后端连接与心跳、登录与 2FA、启动等待页、实时日志、带动效的好友列表、设置、状态图表、SSE 事件总线与外壳交互。 — [模块 29 · API 74]
      - vrcnotifier.web.app.api-client — 请求封装与视图切换 / Request Helper & View Switching — 所有功能共用的唯一请求入口。它带上 Bearer 令牌，并按报文把 401 分成两种：未登录类会让用户回到登录页但不打断事件流；其余 401 会停止事件流并弹出… — [模块 1 · API 3]
      - vrcnotifier.web.app.boot — 启动等待页 / Boot Overlay — 账号上线期间展示的四行等待浮层：语义状态机、让百分比滚动的数字条、逐行光环动画，以及主界面入场重放。 — [模块 5 · API 12]
        - vrcnotifier.web.app.boot.entrance — 头像预热与入场 / Avatar Prewarm & Entrance — 面板出现前的最后一步，存在的意义就是让过渡不显得断裂。它会等待真正会出现在首屏的图片，只统计视口内且不处于折叠分组中的行，并有固定预算，使一个慢头像拖不住整个启… — [模块 1 · API 3]
        - vrcnotifier.web.app.boot.halo — 启动光环动画 / Boot Halo Animation — 每个进行中的行带一圈呼吸光环，而收尾比移除一个 CSS 类要讲究：先读回正在播放的动画当前 transform，再在短时长内收到 scale(1)并用 forw… — [模块 1 · API 2]
        - vrcnotifier.web.app.boot.percent-odometer — 百分比里程表 / Percentage Odometer — 百分比由三条数字带构成的里程表实现，靠 transform 动画推进，并做节流以免数字带糊成一团，但始终强制精确落到目标值。两条规则让它保持诚实：数值只增不减；… — [模块 1 · API 3]
        - vrcnotifier.web.app.boot.state-machine — 启动状态机 / Boot State Machine — 浮层由服务端的语义阶段驱动，而不是客户端自己的请求序列，因为密码直登与 2FA 的请求序列不同，绑请求会让显示错位。它刻意不着急：一行只有在完成标记已置、已停留… — [模块 1 · API 4]
      - vrcnotifier.web.app.bootstrap — 启动引导与地址发现 / Bootstrap & Address Discovery — 启动与地址发现。加载时先从 sessionStorage 同步恢复上次视图与页签，使首帧就是正确的屏幕；随后探测后端：一体化部署时同源优先，其次是已存地址，最后… — [模块 1 · API 4]
      - vrcnotifier.web.app.connection-gate — 连接门禁与心跳 / Connection Gate & Heartbeat — 应用之前的门，以及门后的看门狗。门禁收集协议、主机、端口与令牌，向后端校验通过后才展示登录页。进入之后由 4 秒心跳持续看守：后端不应答时弹出重连弹窗，但带冷却… — [模块 1 · API 3]
      - vrcnotifier.web.app.dropdown — 自定义下拉与筛选持久化 / Custom Dropdown & Filter Persistence — 自定义多选下拉：隐藏原生元素但保留其为唯一事实来源，因此周边代码读写仍是普通 select，而用户看到的是带动画的玻璃拟态菜单。它支持键盘导航并在点击外部时关闭… — [模块 1 · API 2]
      - vrcnotifier.web.app.favorite-flip — 特别关注 FLIP 动画 / Favorite Toggle FLIP Animation — 切换特别关注时的 FLIP 动画。它不是重渲染后让行瞬移到另一个分组，而是先捕获旧矩形、更新 DOM，再把每个移动过的行从旧位置动画到新位置——分组标题与组体也… — [模块 1 · API 3]
      - vrcnotifier.web.app.friend-config — 好友配置提交 / Friend Config Commit — 提交逐好友开关。开关立即响应、请求在后台发出，因为先等一次往返再让复选框动起来会显得卡死；若服务端拒绝，则把开关与缓存的好友对象回滚到旧值并提示错误。整个列表使… — [模块 1 · API 1]
      - vrcnotifier.web.app.groups — 好友分组与搜索 / Friend Groups & Search — 把名册分成若干区块，特别关注提升到最前，离线分组默认折叠，并把搜索框作为跨昵称、世界与状态的筛选器。折叠状态会持久化，使面板回到用户离开时的样子；搜索交互做了去… — [模块 1 · API 2]
      - vrcnotifier.web.app.log-viewer — 后端日志面板 / Backend Log Panel — 后端日志面板。日志行被解析为时间、级别、分类与正文，以便级别着色、分类打徽章；两个多选下拉驱动服务端筛选，因此连客户端从未缓存过的历史也能翻出来。滚到顶部时向后… — [模块 1 · API 4]
      - vrcnotifier.web.app.login — 登录、2FA 与登出 / Login, 2FA & Logout — 登录面及其后续。表单把密码错误、限流与需要邮箱验证三种情况用不同文案区分，2FA 挑战则原地换成验证码表单。登出是带两个独立勾选项的弹窗——清缓存与清设置，因为… — [模块 1 · API 5]
      - vrcnotifier.web.app.monitor-toolbar — 工具栏动作与聚光 / Toolbar Actions & Spotlight — 名册周边的细小交互：刷新按钮调用手动对账端点并如实反馈结果，而不是假装成功；工具栏提示按定时淡出。聚光效果跟随指针，柔和地高亮指针所在的好友行，使密集列表更易读… — [模块 1 · API 3]
      - vrcnotifier.web.app.presence-motion — 上下线差分动画 / Presence Diff Animation — 针对实时在线状态更新的纯客户端差分。刷新时把新数据与当前 DOM 对比：更换分组的行飞过去，没换组的行只把状态文案翻动；整个过程压在一个很短的合并窗口内，使一串… — [模块 1 · API 3]
      - vrcnotifier.web.app.roster — 好友数据与渲染 / Friend Data & Rendering — 好友数据加载与逐人渲染片段。世界名比名册晚到，因为它是按需解析的，因此本模块还负责定点更新：世界名事件到达时，把名字补进正在展示该世界的那几行——列表从不为了等… — [模块 1 · API 3]
      - vrcnotifier.web.app.settings — 设置面板 / Settings Panel — 设置页。由于服务端从不返回 QQ AppSecret，密码框填充的是掩码哨兵；原样提交会被服务端解释为保持原值，因此保存无关设置永远不会删掉凭据。机器人总开关关… — [模块 1 · API 4]
      - vrcnotifier.web.app.shell — 外壳交互 / Shell Interactions — 让面板像应用而非网页的全部细节：带滑动指示块的页签导航、可点的概览卡片、主题切换与外链委托。 — [模块 4 · API 7]
        - vrcnotifier.web.app.shell.overview — 概览卡片与回到顶部 / Overview Cards & Back to Top — 概览条的行为：点击卡片会让该项闪一下，使点击即使没改变底层数值也有可见后果；回到顶部按钮在滚动超过阈值时出现和消失，而不是一动就冒出来。这些细节之所以重要，多半… — [模块 1 · API 2]
        - vrcnotifier.web.app.shell.tabs — 页签导航 / Tab Navigation — 页签切换保留了一个有意的不对称：用户点击时播放快速交叉淡入与卡片级联入场，而加载时恢复上次页签则静态渲染，避免每次刷新都对着用户放一遍动画。指示块只在容器可见时… — [模块 1 · API 3]
        - vrcnotifier.web.app.shell.theme — 主题切换 / Theme Switching — 主题切换在显式的浅色与深色之外还提供跟随系统，且“自动”真的跟随系统变化而不是只猜一次：系统偏好改变时会重新求值。选择持久化在内联头部脚本读取的同一个键上，因此… — [模块 1 · API 2]
      - vrcnotifier.web.app.smooth-scroll — 惯性平滑滚动 / Inertial Smooth Scrolling — 用惯性曲线替换浏览器默认滚轮滚动，使可能承载上千行的日志面板不会每滚一格就一顿。它还把这套缓动暴露为程序化滚动目标，使回到顶部与页签跳转的观感与手动滚动一致，而… — [模块 1 · API 2]
      - vrcnotifier.web.app.sse-events — SSE 事件订阅 / SSE Event Subscription — 事件流的客户端一半：每个会话一个 EventSource，按命名事件逐一绑定处理函数，覆盖日志及其就地更新、状态、健康、图表数据、登录进度、通知、快照、世界名、… — [模块 1 · API 1]
      - vrcnotifier.web.app.status — 状态徽章 / Status Badges — 常驻概览条及其中的徽章。状态经事件流到达而非轮询，因此后端一看到变化，面板就跟着变。服务状态渲染为徽章，悬停可看到受影响组件列表；延迟按阈值着色，使 API 在… — [模块 1 · API 4]
      - vrcnotifier.web.app.ws-chart — WebSocket 速率图表 / WebSocket Rate Chart — WebSocket 消息速率图表：在帧循环中平滑左移的 canvas、带停留延迟的悬停交互，以及把它锚定到真实时间的秒级数据流。 — [模块 3 · API 8]
        - vrcnotifier.web.app.ws-chart.canvas — 图表画布与数据流 / Chart Canvas & Feed — 图表锚定到墙上时间而非到达顺序，因此繁忙的事件循环漏掉一次 tick 也不会悄悄把整条序列错位：ticker 会重发最近几秒，客户端按秒幂等合并。渐变取自解析后… — [模块 1 · API 4]
        - vrcnotifier.web.app.ws-chart.hover-tip — 图表悬停提示 / Chart Hover Tooltip — 让速率图表的悬停不闪烁。停留延迟使指针扫过图表时不会闪出一串提示；宽限期则桥接柱与柱之间的一像素空隙，使指针跨过空隙时提示不会消失。提示是挂在 body 上的元… — [模块 1 · API 4]
    - vrcnotifier.web.logview — 日志窗口裁剪策略 / Log Window Trimming Policy — 日志面板的裁剪策略，独立成模块以便脱离浏览器做单元测试。它感知方向：实时模式下新行从顶部插入，因此裁掉底部最旧的行；向前翻页时旧行追加到底部，因此裁掉顶部最新的… — [模块 1 · API 2]
    - vrcnotifier.web.sdk — 前端 SDK 客户端 / Frontend SDK Client — 后端可选客户端库，浏览器中作为全局使用，也可在 Node 中 require。它为 REST 面每个端点提供方法，自动带上 Bearer 令牌，把失败归一为带状… — [模块 1 · API 4]
    - vrcnotifier.web.shell — 页面骨架与主题样式 / Page Shell & Theme — 其他一切挂载其上的静态骨架：三个顶层视图（连接门禁、登录、主界面）、两个主页签、五个浮层，以及承载设计令牌、本地字体与两套主题的样式表。除一段在首帧前应用已存主… — [模块 1 · API 3]
    - vrcnotifier.web.vrclinks — VRChat 外链构造 / VRChat Deep Links — 为头像、昵称与世界名构造 VRChat 深链。标识来自服务端，因此写入属性前一律经严格白名单校验；不合法者退化为已转义的纯文本，而不是生成一个坏链。链接刻意用 … — [模块 1 · API 4]

