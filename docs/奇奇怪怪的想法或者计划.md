# 1.状态变化细化
6种状态细化  
(准确是5种,毕竟谁tm需要active到offline啊)

# 2.好友日志
- 类似vrcx一样的好友日志
- 仅用现有数据存档
# 3.登陆后启动前端等待动画/页面

# 4.版本管理

## 自动化版本发布管理 ✅ 已完成
- git tag是唯一真相: src/version.js 算下一个patch号, release.yml 每次push main自动 打tag→发镜像→建Release(44f4e2e)
- 发布说明自动附自上一个版本以来的提交列表; 纯文档/CI改动 paths-ignore 不发版不烧版本号
- 手工推 v* tag 发 major/minor 时由 docker-publish.yml 构建镜像并建Release

## 前后端/数据库版本自校验

- 统一前端可选择版本
- 数据库/前后端自动化带上版本号/git哈希,保证一致性

# 5.更符合人类的api鉴权方式

## 账号密码
1. vrc账号密码
2. 自设全局管理账号密码
## 首次启动鉴权
1. 首次免鉴权?

# 6.后端托管前端 ✅ 已完成
~~不知道为什么当时想着要分成两个容器~~ — 已收敛为单容器:`SERVE_STATIC=1`,面板与 `/api` 同源同端口;nginx 前端容器已下架(历史镜像保留),公共前端不再在 README 推广。
# 7.群组通知精细化管理
可选通知的群组
# 8.前端功能(已完成285ddbee1967835268a7c688fad84d4684ebca76)
- 点击好友头像/名字/世界等打开vrc网页
# 9.软件包
- npm
- fpk (飞牛的包)
# 10.预览页面
一个假的预览页面,打开可预览项目效果

# 11.我要写啥来着

# 12.关于页面
- 前端加一个「关于」页,三块内容:随时公告 / 发布说明 / 版本信息
- 随时公告:仓库里放 announcement.json,想发就改了提交,不用发版
- 发布说明:GitHub Releases API 拉 release notes,发版即自带
- 统一由后端拉取(前端不直连github,国内raw不稳定):面板走 /api/announcement + SSE
- 强提醒24h内送到就行 → 后端每小时轮询一次公告+最新release(条件GET一天24次,成本忽略),同一timer
- 强提醒触发:公告带push字段(level:alert也行)→ QQ推给所有绑定用户+SSE广播;已推哈希存DB防重启重推
- 版本更新探测:同轮询比对最新release tag与自身版本(只比基础semver,dev后缀不算旧),有新版→QQ推一次+面板横幅+关于页提示;settings加开关可关;已提醒tag存DB
- 登录页/主界面可关横幅,关闭记localStorage